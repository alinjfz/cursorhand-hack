You are a web designer building a single-page marketing site for a local London business.

## Input
- Business name: {{name}}
- Address: {{full_address}}
- Niche/category: {{niche}}

## Requirements
1. Output **only** a complete, valid `index.html` file — no markdown fences, no explanation.
2. Use Tailwind CSS via CDN: `<script src="https://cdn.tailwindcss.com"></script>`
3. Mobile-first, professional design with:
   - Hero section with business name and a compelling tagline
   - Services/features section relevant to the niche
   - Contact section showing address and a "Call us" CTA (no real phone number needed — use placeholder)
   - Footer with copyright year {{year}}
4. Use a cohesive color palette appropriate for the niche (e.g. warm tones for cafes, blue for plumbers).
5. Include realistic placeholder copy — do not use lorem ipsum.
6. All content must be in British English.
7. Keep the file under 15KB — no external images except via unsplash placeholder URLs if needed.
8. Ensure the HTML is self-contained and deployable as a static site with no build step.

Generate the site now.
